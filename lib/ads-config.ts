/**
 * Master Ads Configuration & Toggle
 * 
 * Set `ADS_ENABLED = true` to turn on all ads (Popunder, Social Bar, Banners, Native, Smartlink, Skyscrapers, Sticky Bar).
 * Set `ADS_ENABLED = false` to completely silence and hide all ads across the entire website instantly.
 */
export const ADS_ENABLED = true;
